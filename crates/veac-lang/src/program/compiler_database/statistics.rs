#[derive(Debug, Clone, Copy, Default, PartialEq, Eq)]
pub struct CompilerDatabaseStatistics {
    pub syntax_hits: u64,
    pub syntax_misses: u64,
    pub syntax_insertions: u64,
    pub syntax_evictions: u64,
    pub syntax_bypasses: u64,
    pub cached_syntax_entries: usize,
    pub cached_syntax_source_bytes: usize,
    pub interface_hits: u64,
    pub interface_misses: u64,
    pub interface_insertions: u64,
    pub interface_evictions: u64,
    pub interface_bypasses: u64,
    pub cached_interface_entries: usize,
    pub cached_interface_retained_bytes: usize,
    pub hir_hits: u64,
    pub hir_misses: u64,
    pub hir_insertions: u64,
    pub hir_evictions: u64,
    pub hir_bypasses: u64,
    pub cached_hir_entries: usize,
    pub cached_hir_retained_bytes: usize,
    pub core_hits: u64,
    pub core_misses: u64,
    pub core_insertions: u64,
    pub core_evictions: u64,
    pub core_bypasses: u64,
    pub cached_core_entries: usize,
    pub cached_core_retained_bytes: usize,
    pub semantic_invalidations: u64,
    pub pending_semantic_invalidations: usize,
    pub dependency_route_edges: usize,
    pub dependency_retained_entries: usize,
    pub dependency_retained_bytes: usize,
    pub dependency_bypasses: u64,
    pub dependency_resets: u64,
    pub dependency_evictions: u64,
}
