// @generated by scripts/stdlib_codegen.py; do not edit.
use super::super::builders::*;
use crate::program::domain_system::{
    DomainOperationContract as Contract, DomainOperationId as Op, DomainRuntimeAction as Action,
    DomainType as Type,
};
use crate::program::expression::PrimitiveType;

pub(super) fn contract(id: Op) -> Option<Contract> {
    Some(match id {
        Op::ColorWheel => build(
            id,
            free("color_wheel"),
            vec![
                leaf("red", primitive(PrimitiveType::Scalar)),
                leaf("green", primitive(PrimitiveType::Scalar)),
                leaf("blue", primitive(PrimitiveType::Scalar)),
            ],
            Type::ColorWheel,
            Action::Description,
        ),
        Op::LiftGammaGain => build(
            id,
            free("lift_gamma_gain"),
            vec![
                leaf("lift", domain(Type::ColorWheel)),
                leaf("gamma", domain(Type::ColorWheel)),
                leaf("gain", domain(Type::ColorWheel)),
            ],
            Type::LiftGammaGain,
            Action::Description,
        ),
        Op::LutNearest => build(
            id,
            free("lut_nearest"),
            vec![],
            Type::LutInterpolation,
            Action::Description,
        ),
        Op::LutLinear => build(
            id,
            free("lut_linear"),
            vec![],
            Type::LutInterpolation,
            Action::Description,
        ),
        Op::LutCosine => build(
            id,
            free("lut_cosine"),
            vec![],
            Type::LutInterpolation,
            Action::Description,
        ),
        Op::LutCubic => build(
            id,
            free("lut_cubic"),
            vec![],
            Type::LutInterpolation,
            Action::Description,
        ),
        Op::LutSpline => build(
            id,
            free("lut_spline"),
            vec![],
            Type::LutInterpolation,
            Action::Description,
        ),
        Op::LutTrilinear => build(
            id,
            free("lut_trilinear"),
            vec![],
            Type::LutInterpolation,
            Action::Description,
        ),
        _ => return None,
    })
}
