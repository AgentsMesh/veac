// @generated by scripts/stdlib_codegen.py; do not edit.
use super::super::builders::*;
use crate::program::domain_system::{
    DomainOperationContract as Contract, DomainOperationId as Op, DomainRuntimeAction as Action,
    DomainType as Type,
};

pub(super) fn contract(id: Op) -> Option<Contract> {
    Some(match id {
        Op::TrackIsolationNormal => build(
            id,
            free("track_isolation_normal"),
            vec![],
            Type::TrackIsolation,
            Action::Description,
        ),
        Op::TrackIsolationSolo => build(
            id,
            free("track_isolation_solo"),
            vec![],
            Type::TrackIsolation,
            Action::Description,
        ),
        Op::TrackEditingUnlocked => build(
            id,
            free("track_editing_unlocked"),
            vec![],
            Type::TrackEditing,
            Action::Description,
        ),
        Op::TrackEditingLocked => build(
            id,
            free("track_editing_locked"),
            vec![],
            Type::TrackEditing,
            Action::Description,
        ),
        Op::TrackState => build(
            id,
            free("track_state"),
            vec![
                topology("playback", domain(Type::TrackPlayback)),
                leaf("audio", domain(Type::TrackAudioState)),
                topology("isolation", domain(Type::TrackIsolation)),
                topology("editing", domain(Type::TrackEditing)),
            ],
            Type::TrackState,
            Action::Description,
        ),
        Op::TrackRoutingDefault => build(
            id,
            free("track_routing_default"),
            vec![],
            Type::TrackRouting,
            Action::Description,
        ),
        Op::TrackRoutingBus => build(
            id,
            free("track_routing_bus"),
            vec![leaf("bus", domain(Type::AudioBus))],
            Type::TrackRouting,
            Action::Description,
        ),
        Op::ItemEnabled => build(
            id,
            free("item_enabled"),
            vec![],
            Type::ItemState,
            Action::Description,
        ),
        _ => return None,
    })
}
