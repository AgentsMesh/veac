// @generated by scripts/stdlib_codegen.py; do not edit.
use super::super::builders::*;
use crate::program::domain_system::{
    DomainOperationContract as Contract, DomainOperationId as Op, DomainRuntimeAction as Action,
    DomainType as Type,
};

pub(super) fn contract(id: Op) -> Option<Contract> {
    Some(match id {
        Op::LutTetrahedral => build(
            id,
            free("lut_tetrahedral"),
            vec![],
            Type::LutInterpolation,
            Action::Description,
        ),
        Op::LutPyramid => build(
            id,
            free("lut_pyramid"),
            vec![],
            Type::LutInterpolation,
            Action::Description,
        ),
        Op::LutPrism => build(
            id,
            free("lut_prism"),
            vec![],
            Type::LutInterpolation,
            Action::Description,
        ),
        Op::LutApplication => build(
            id,
            free("lut_application"),
            vec![
                topology("resource", domain(Type::Resource)),
                leaf("interpolation", domain(Type::LutInterpolation)),
            ],
            Type::LutApplication,
            Action::Description,
        ),
        Op::ColorStageBasic => build(
            id,
            free("color_stage_basic"),
            vec![leaf("value", domain(Type::BasicColorAdjustment))],
            Type::ColorStage,
            Action::Description,
        ),
        Op::ColorStageMatrix => build(
            id,
            free("color_stage_matrix"),
            vec![leaf("value", domain(Type::RgbMatrixAdjustment))],
            Type::ColorStage,
            Action::Description,
        ),
        Op::ColorStageHsl => build(
            id,
            free("color_stage_hsl"),
            vec![leaf("value", domain(Type::HslAdjustment))],
            Type::ColorStage,
            Action::Description,
        ),
        Op::ColorStageCurves => build(
            id,
            free("color_stage_curves"),
            vec![leaf("value", domain(Type::ColorCurves))],
            Type::ColorStage,
            Action::Description,
        ),
        _ => return None,
    })
}
