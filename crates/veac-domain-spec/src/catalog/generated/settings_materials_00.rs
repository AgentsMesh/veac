// @generated by scripts/stdlib_codegen.py; do not edit.
use super::super::builders::*;
use crate::{
    DomainInstructionKind as Instruction, DomainOperationContract as Contract,
    DomainOperationId as Op, DomainRuntimeAction as Action, DomainType as Type,
};
use veac_lang_model::PrimitiveType;
use veac_lang_model::{Effect, Stage};

pub(super) fn contract(id: Op) -> Option<Contract> {
    Some(match id {
        Op::ProjectSettings => build(
            id,
            free("project_settings"),
            vec![topology("timebase_hz", primitive(PrimitiveType::Integer))],
            Type::ProjectSettings,
            semantics(
                Instruction::DomainConstruct,
                Action::Description,
                Effect::Pure,
                Stage::Build,
                None,
            ),
        ),
        Op::SequenceSettings => build(
            id,
            free("sequence_settings"),
            vec![
                topology("canvas", domain(Type::Canvas)),
                topology("frame_rate", domain(Type::FrameRate)),
                topology("sample_rate_hz", primitive(PrimitiveType::Integer)),
            ],
            Type::SequenceSettings,
            semantics(
                Instruction::DomainConstruct,
                Action::Description,
                Effect::Pure,
                Stage::Build,
                None,
            ),
        ),
        Op::PlacementMagnetic => build(
            id,
            free("placement_magnetic"),
            vec![],
            Type::PlacementMode,
            semantics(
                Instruction::DomainConstruct,
                Action::Description,
                Effect::Pure,
                Stage::Build,
                None,
            ),
        ),
        Op::PlacementFree => build(
            id,
            free("placement_free"),
            vec![],
            Type::PlacementMode,
            semantics(
                Instruction::DomainConstruct,
                Action::Description,
                Effect::Pure,
                Stage::Build,
                None,
            ),
        ),
        Op::TrackPlaybackEnabled => build(
            id,
            free("track_playback_enabled"),
            vec![],
            Type::TrackPlayback,
            semantics(
                Instruction::DomainConstruct,
                Action::Description,
                Effect::Pure,
                Stage::Build,
                None,
            ),
        ),
        Op::TrackPlaybackDisabled => build(
            id,
            free("track_playback_disabled"),
            vec![],
            Type::TrackPlayback,
            semantics(
                Instruction::DomainConstruct,
                Action::Description,
                Effect::Pure,
                Stage::Build,
                None,
            ),
        ),
        Op::TrackAudioAudible => build(
            id,
            free("track_audio_audible"),
            vec![],
            Type::TrackAudioState,
            semantics(
                Instruction::DomainConstruct,
                Action::Description,
                Effect::Pure,
                Stage::Build,
                None,
            ),
        ),
        Op::TrackAudioMuted => build(
            id,
            free("track_audio_muted"),
            vec![],
            Type::TrackAudioState,
            semantics(
                Instruction::DomainConstruct,
                Action::Description,
                Effect::Pure,
                Stage::Build,
                None,
            ),
        ),
        _ => return None,
    })
}
