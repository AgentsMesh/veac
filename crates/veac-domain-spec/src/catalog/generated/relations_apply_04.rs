// @generated by scripts/stdlib_codegen.py; do not edit.
use super::super::builders::*;
use crate::{
    DomainInstructionKind as Instruction, DomainOperationContract as Contract,
    DomainOperationId as Op, DomainRuntimeAction as Action, DomainType as Type,
};
use veac_lang_model::{Effect, Stage};

pub(super) fn contract(id: Op) -> Option<Contract> {
    Some(match id {
        Op::ApplyTargetLayer => build(
            id,
            free("apply_target_layer"),
            vec![topology("layer", domain(Type::Layer))],
            Type::ApplyTarget,
            semantics(
                Instruction::DomainConstruct,
                Action::Description,
                Effect::Pure,
                Stage::Build,
                None,
            ),
        ),
        Op::ApplyTargetItems => build(
            id,
            free("apply_target_items"),
            vec![topology("items", domain_list(Type::Item))],
            Type::ApplyTarget,
            semantics(
                Instruction::DomainConstruct,
                Action::Description,
                Effect::Pure,
                Stage::Build,
                None,
            ),
        ),
        Op::ApplyEnabled => build(
            id,
            free("apply_enabled"),
            vec![],
            Type::ApplyState,
            semantics(
                Instruction::DomainConstruct,
                Action::Description,
                Effect::Pure,
                Stage::Build,
                None,
            ),
        ),
        Op::ApplyDisabled => build(
            id,
            free("apply_disabled"),
            vec![],
            Type::ApplyState,
            semantics(
                Instruction::DomainConstruct,
                Action::Description,
                Effect::Pure,
                Stage::Build,
                None,
            ),
        ),
        Op::ApplyStageWindowFull => build(
            id,
            free("apply_stage_window_full"),
            vec![],
            Type::ApplyStageWindow,
            semantics(
                Instruction::DomainConstruct,
                Action::Description,
                Effect::Pure,
                Stage::Build,
                None,
            ),
        ),
        Op::ApplyStageWindowDuring => build(
            id,
            free("apply_stage_window_during"),
            vec![leaf("range", domain(Type::TimeRange))],
            Type::ApplyStageWindow,
            semantics(
                Instruction::DomainConstruct,
                Action::Description,
                Effect::Pure,
                Stage::Build,
                None,
            ),
        ),
        Op::ApplyStageEnabled => build(
            id,
            free("apply_stage_enabled"),
            vec![leaf("window", domain(Type::ApplyStageWindow))],
            Type::ApplyStageState,
            semantics(
                Instruction::DomainConstruct,
                Action::Description,
                Effect::Pure,
                Stage::Build,
                None,
            ),
        ),
        Op::ApplyStageDisabled => build(
            id,
            free("apply_stage_disabled"),
            vec![leaf("window", domain(Type::ApplyStageWindow))],
            Type::ApplyStageState,
            semantics(
                Instruction::DomainConstruct,
                Action::Description,
                Effect::Pure,
                Stage::Build,
                None,
            ),
        ),
        _ => return None,
    })
}
