// @generated by scripts/stdlib_codegen.py; do not edit.
use super::super::builders::*;
use crate::{
    DomainInstructionKind as Instruction, DomainOperationContract as Contract,
    DomainOperationId as Op, DomainRuntimeAction as Action, DomainType as Type,
};
use veac_lang_model::PrimitiveType;
use veac_lang_model::{Effect, Stage};

pub(super) fn contract(id: Op) -> Option<Contract> {
    Some(match id {
        Op::DeliverableCaptionSidecar => build(
            id,
            free("deliverable_caption_sidecar"),
            vec![
                topology("key", primitive(PrimitiveType::Identifier)),
                topology("target", domain(Type::DeliverableTarget)),
                leaf("format", domain(Type::CaptionSidecarFormat)),
                topology("layers", domain_list(Type::Layer)),
            ],
            Type::Deliverable,
            semantics(
                Instruction::DomainConstruct,
                Action::Description,
                Effect::Pure,
                Stage::Build,
                None,
            ),
        ),
        Op::StemWav => build(
            id,
            free("stem_wav"),
            vec![],
            Type::AudioStemFormat,
            semantics(
                Instruction::DomainConstruct,
                Action::Description,
                Effect::Pure,
                Stage::Build,
                None,
            ),
        ),
        Op::StemFlac => build(
            id,
            free("stem_flac"),
            vec![],
            Type::AudioStemFormat,
            semantics(
                Instruction::DomainConstruct,
                Action::Description,
                Effect::Pure,
                Stage::Build,
                None,
            ),
        ),
        Op::MixMaster => build(
            id,
            free("mix_master"),
            vec![],
            Type::AudioMixSource,
            semantics(
                Instruction::DomainConstruct,
                Action::Description,
                Effect::Pure,
                Stage::Build,
                None,
            ),
        ),
        Op::MixLayer => build(
            id,
            free("mix_layer"),
            vec![topology("layer", domain(Type::Layer))],
            Type::AudioMixSource,
            semantics(
                Instruction::DomainConstruct,
                Action::Description,
                Effect::Pure,
                Stage::Build,
                None,
            ),
        ),
        Op::MixBus => build(
            id,
            free("mix_bus"),
            vec![leaf("bus", domain(Type::AudioBus))],
            Type::AudioMixSource,
            semantics(
                Instruction::DomainConstruct,
                Action::Description,
                Effect::Pure,
                Stage::Build,
                None,
            ),
        ),
        Op::ScopeWaveform => build(
            id,
            free("scope_waveform"),
            vec![],
            Type::VideoScope,
            semantics(
                Instruction::DomainConstruct,
                Action::Description,
                Effect::Pure,
                Stage::Build,
                None,
            ),
        ),
        Op::ScopeVectorscope => build(
            id,
            free("scope_vectorscope"),
            vec![],
            Type::VideoScope,
            semantics(
                Instruction::DomainConstruct,
                Action::Description,
                Effect::Pure,
                Stage::Build,
                None,
            ),
        ),
        _ => return None,
    })
}
